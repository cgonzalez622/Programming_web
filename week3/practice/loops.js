
/* const colors = ["red", "blue", "yellow", "green"]; */
// first define a loop var, second det limit, third change loop var
// increment by 1 > varName++
// decrement by 1 > varName--
// other number changes is math operator + equal (varName+=#, *=#, etc
/* for(let loopVar = 0; loopVar < colors.length; loopVar++) {
    console.log("loopVar", colors[loopVar]);
}  */

//let loopVar = 0 giving us a variable starting with 0 of the array's index
// loopVar < colors.length, when loopVar gets to be like < then kill the loop
// loopVar++ the next time it comes back 

/* for(let i = 200; i < 400; i+= 10) {
    console.log("i is", i);
} */

/* let myNumber = 0;
while(myNumber <= 200) {
    console.log("myNumber", myNumber);
    myNumber += 10;
} */

